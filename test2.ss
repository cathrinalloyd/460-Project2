(define (isNumber num)
  (number? num)
)

(define (booleanTests a b)
  (and (number? a) (number? b)
    (> a b)
    (< a b)
    (>= a b)
    (<= a b)
    (string? 'hello)
    (zero? a)
  )
  (or (not (char? a)) (not (char? b))
    (display 'ok)
    (newline)
  )
)

